# front

React + reducer + redux-saga

# back

JavaScript + Express + PostgreSQL

# cloud && web server

aws s3 + nginx

### `Initialize PostgreSQL`

```
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
```

### `npm start`

### `npm test`

### `npm run build`
